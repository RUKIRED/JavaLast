<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sample.common.dao.mapper.LoginMapper">
  <resultMap id="LoginMap" type="sample.common.dao.entity.LoginUser">
    <id column="id" property="id" ></id>
    <result column="username" property="username" />
    <result column="password" property="password" />
    <result column="created_at" property="createdAt" />
    <result column="updated_at" property="updatedAt" ></result>
  </resultMap>

  <select id="findByUsername" resultMap="LoginMap">
    SELECT * FROM login
    WHERE username = #{username}
  </select>

  <insert id="insert" parameterType="sample.common.dao.entity.LoginUser" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO login (
      username, password
    ) VALUES (
      #{username}, #{password}
    )
  </insert>
</mapper>
